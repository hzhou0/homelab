{{- if .Values.policies.enabled }}
apiVersion: policies.kyverno.io/v1
kind: ValidatingPolicy
metadata:
  name: allowed-kinds-app
  labels:
{{ include "platform.labels" . | indent 4 }}
  annotations:
    policies.kyverno.io/title: Allowed resource kinds (app-*)
    policies.kyverno.io/description: >-
      app-* namespaces follow the standard deploy pattern. Stateful and batch kinds
      are rejected here (use a tool-* namespace). Claims are governed separately:
      the app tier may hold them only on a shared-filesystem StorageClass.
spec:
  validationActions: [{{ .Values.policies.validationAction | default "Deny" }}]
  evaluation:
    background:
      enabled: false
  matchConstraints:
    resourceRules:
      - apiGroups: ["apps"]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["statefulsets", "daemonsets", "replicasets"]
      - apiGroups: ["batch"]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["jobs", "cronjobs"]
      - apiGroups: [""]
        apiVersions: ["v1"]
        operations: ["CREATE", "UPDATE"]
        resources:
          - "replicationcontrollers"
{{- if not .Values.storageClasses }}
          - "persistentvolumeclaims"
{{- end }}
  matchConditions:
    - name: in-app-namespace
      expression: "object.metadata.namespace.startsWith('{{ .Values.prefixes.app }}')"
  validations:
    - expression: "false"
      messageExpression: >-
        "'" + object.kind + "' is not allowed in app-* namespaces (Deployments only); use a tool-* namespace."
{{- end }}
