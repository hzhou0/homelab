{{- if .Values.gateway.enabled }}
# HTTPRoutes exposing SeaweedFS UIs through the shared ingress Gateway. Each sits next to its Service
# (this namespace, no ReferenceGrant needed) and attaches to the `internal` Gateway; reachability is
# bounded by the gateway's L3 allow-list.
{{- range $route := .Values.gateway.routes }}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ $route.name }}
  namespace: {{ $.Release.Namespace }}
  labels:
    app.kubernetes.io/managed-by: {{ $.Release.Service }}
    app.kubernetes.io/part-of: homelab-seaweedfs
spec:
  parentRefs:
    - name: {{ $.Values.gateway.name }}
      namespace: {{ $.Values.gateway.namespace }}
  hostnames:
    - {{ $route.hostname | quote }}
  rules:
    - backendRefs:
        - name: {{ $route.serviceName }}
          port: {{ $route.servicePort }}
{{- end }}
{{- end }}
